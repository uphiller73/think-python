[project]
name = "think-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Apip Kamil", email = "apip.kamil@icloud.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
think-python = "think_python:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
